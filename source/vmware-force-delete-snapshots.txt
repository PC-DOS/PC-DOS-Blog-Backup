首先，请导航到虚拟机所在目录，并删除下列文件：

* 扩展名为{{vmsd}}的文件，它们通常被描述为“快照元数据文件”。
* 扩展名为{{vmsn}}的文件，它们通常被描述为“虚拟机快照文件”。
* 文件名包含“{{Snapshot}}”、且扩展名为{{vmem}}的文件，它们是热快照的内存状态文件。

之后，您可以考虑进行一次[[[vmware-merge-virtual-disk|虚拟磁盘合并]]]以节约空间。