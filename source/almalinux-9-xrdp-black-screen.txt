编辑{{/etc/xrdp/sesman.ini}}文件，将{{KillDisconnected}}的值改为{{true}}：

[[code]]
KillDisconnected=true
[[/code]]

随后重新启动系统。

您也可以使用{{htop}}等工具杀掉所有{{/usr/libexec/gnome-session-binary}}进程，并重启{{xrdp}}服务：

[[code]]
sudo systemctl restart xrdp
[[/code]]
